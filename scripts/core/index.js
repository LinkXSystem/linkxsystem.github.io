const elf = function(option, callback) {
  const _option = option;

  const entry = option.entry;
  const output = option.output;

  const modules = option.module || {};
  const plugins = option.plugins || [];
};

module.exports = elf;
